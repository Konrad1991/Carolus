#ifndef UPDATE_TREE_H
#define UPDATE_TREE_H

#include "types.h"

void update_trees(ObjectArray *objects, const Texture_State *texture_state, const WeatherState *weather_state, SeasonBlend season_blend);

#endif
